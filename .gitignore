# ---- Runtime logs (serve_local.py / bridge background runs) ----
*.log
*.err

# ---- Secrets / config ----
*.env
.env
**/google-services.json
**/GoogleService-Info.plist
**/secrets.h
**/cam_secrets.h
**/wifi_config.h

# NOTE: lib/firebase_options.dart is committed with PLACEHOLDER values so the
# app always compiles. Replace it locally via `flutterfire configure`.
# Firebase client config is not a server secret, but do not commit a config
# tied to a production project.

# ---- Flutter / Dart ----
mobile/smart_fridge_app/.dart_tool/
mobile/smart_fridge_app/.flutter-plugins
mobile/smart_fridge_app/.flutter-plugins-dependencies
mobile/smart_fridge_app/.packages
mobile/smart_fridge_app/build/
mobile/smart_fridge_app/.pub-cache/
mobile/smart_fridge_app/.pub/
mobile/smart_fridge_app/android/.gradle/
mobile/smart_fridge_app/ios/Pods/
mobile/smart_fridge_app/**/GeneratedPluginRegistrant.*

# ---- Arduino ----
firmware/**/build/

# ---- OS / Editor ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.iml
