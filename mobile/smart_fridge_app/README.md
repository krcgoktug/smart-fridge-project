# Smart Fridge — Flutter App

Mobile dashboard showing live sensor values, camera stream, product
list (from QR scans) and alerts.

## Setup

```bash
flutter pub get
flutterfire configure
flutter run
```

The shell builds out of the box. The full screen graph is restored
from the team archive when wiring against a real Firebase project.
